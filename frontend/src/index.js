import React from 'react';
import ReactDOM from 'react-dom'; /*Importando a biblioteca que permite o React lidar com a DOM*/

import App from './App';

ReactDOM.render(<App />, document.getElementById('root')); /*Renderiza alguma coisa (no caso, <App />) dentro da div cujo ID é root*/

